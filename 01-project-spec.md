# CapsuleOS — نظام تشغيل موحّد للتطوير والنشر

> اسم مقترح مؤقت (يمكن تغييره لاحقًا). هذه الوثيقة هي المرجع الرسمي الكامل للمشروع.

---

## 1. المشكلة الجذرية (لماذا نبني هذا أصلًا)

كل أنظمة التشغيل الحالية (Windows, macOS, Linux بتوزيعاته، حتى الأنظمة السحابية مثل Puter) تفصل بنيويًا بين أربع طبقات منفصلة كل واحدة تحتاج أداة مختلفة:

1. **الكود المصدري** — على جهاز المطوّر، في محرر.
2. **الاعتماديات (dependencies)** — تُدار بأدوات خارجية (npm, pip, apt) بنسخ قد تتضارب بين جهاز وآخر.
3. **بيئة التشغيل الفعلية** — حاوية Docker أو خادم مختلف كليًا عن جهاز التطوير.
4. **النشر/الإنتاج** — منصة سحابية ثالثة بإعدادات خاصة بها (Vercel, AWS, إلخ).

النتيجة العملية لهذا الفصل:
- مشكلة "يشتغل عندي بس مو عالسيرفر" (works on my machine).
- طبقات "غراء" هشة تربط الطبقات ببعضها (CI/CD معقّد، Dockerfiles، ملفات تهيئة متكررة).
- فجوة أمنية بين بيئة البناء وبيئة الإنتاج.
- ساعات ضائعة بالإعداد بدل البرمجة الفعلية.

**لا يوجد نظام تشغيل واحد صُمم من الجذر لإلغاء هذا الفصل.** كل الحلول الموجودة (Docker, Nix, Kubernetes, Heroku) تحاول رأب الفجوة *فوق* نظام تشغيل غير مصمم أصلًا لهذا الغرض. نحن نبني النظام الذي تكون فيه الوحدة يقتناة نفسها.

---

## 2. الحل: مفهوم "الكبسولة" (Capsule)

الوحدة الأساسية الجديدة في النظام (تحل محل ثنائية الملف/العملية التقليدية):

**الكبسولة = الكود + بيان الاعتماديات + الحالة الحية وهي تعمل + معرّف محتوى ثابت (content hash)**

خصائصها:
- **معرّف بالمحتوى (content-addressed)**: نفس الكود ينتج دائمًا نفس المعرّف (مستوحى من مبدأ Nix/IPFS). هذا يلغي "يشتغل عندي" من الجذر لأن أي جهاز يشغّل نفس المعرّف = نفس البيئة تمامًا، حرفيًا.
- **معزولة على مستوى النواة**: لا Docker كطبقة خارجية فوق نظام غير معزول، بل عزل مدمج في مدير النظام نفسه (namespaces + cgroups + seccomp، مع خيار Firecracker microVMs لعزل أقوى لاحقًا).
- **"تشغيل = نشر"**: بمجرد ما تُشغَّل الكبسولة محليًا، تصبح متاحة على الشبكة تلقائيًا عبر نفق مدمج بالنظام (لا حاجة لخطوة نشر منفصلة).

---

## 3. المبادئ التصميمية الخمسة (لا تُنتهك)

1. **لا فصل بين الكود وما يعمل فعليًا.** تعديل الكود = تعديل النظام الحي، بلا خطوة "بناء ثم نشر" منفصلة.
2. **العزل خاصية بنيوية، لا إضافة.** كل كبسولة معزولة تلقائيًا بلا إعداد إضافي من المطوّر.
3. **التطبيقات الأصلية = تقنيات ويب.** أي مطوّر ويب يبني للنظام مباشرة بمهاراته الحالية (فلسفة webOS/Firefox OS).
4. **محلي أولًا، سحابي عند الحاجة.** النظام يعمل بالكامل بدون إنترنت؛ الخدمات السحابية (ذكاء اصطناعي، مزامنة، متجر تطبيقات) طبقة اختيارية إضافية فقط.
5. **مفتوح المصدر بالكامل ومبني بالمجتمع.** لا قرار تصميمي مغلق، كل شيء قابل للنقاش والمساهمة عبر GitHub.

---

## 4. المعمارية الكاملة

```
┌──────────────────────────────────────────────┐
│  طبقة الواجهة (WebView Shell)                 │
│  - كل تطبيق = صفحة ويب، لا سطح مكتب تقليدي     │
│  - محرر + طرفية + مستكشف كبسولات مدمجين        │
├──────────────────────────────────────────────┤
│  مدير الكبسولات (Capsule Manager / init بديل) │
│  - يحل محل systemd، هو PID 1                  │
│  - يدير: البناء، العزل، النفق، دورة الحياة     │
├──────────────────────────────────────────────┤
│  متجر المحتوى (Content Store)                 │
│  - تخزين بمعرّف hash ثابت (مبدأ Nix Store)      │
│  - OverlayFS لتركيب طبقات الكبسولات            │
├──────────────────────────────────────────────┤
│  طبقة العزل (namespaces/cgroups/seccomp        │
│               → Firecracker microVMs لاحقًا)   │
├──────────────────────────────────────────────┤
│  نواة Linux (LTS، غير معدّلة، كما هي)          │
└──────────────────────────────────────────────┘
                      │
                      │ اتصال اختياري (فقط عند وجود إنترنت)
                      ▼
┌──────────────────────────────────────────────┐
│  الخدمات السحابية الخلفية (خادم منفصل عن       │
│  GitHub — لا يمكن لـ GitHub استضافة هذا)       │
│  - مساعد ذكاء اصطناعي (نموذج مفتوح محلي/بعيد)  │
│  - متجر تطبيقات المجتمع + تحديثات              │
│  - مزامنة إعدادات بين الأجهزة                  │
└──────────────────────────────────────────────┘
```

---

## 5. المكدس التقني لكل طبقة

| الطبقة | التقنية | لماذا |
|---|---|---|
| النواة | Linux LTS | لا نعيد اختراع النواة، نركّز الجهد على الابتكار الحقيقي |
| البناء | Buildroot | أبسط وأسرع من Yocto لتوزيعة مخصصة صغيرة |
| init/مدير الكبسولات | Rust | أمان الذاكرة + أداء قريب من C، مناسب لمكوّن حساس بهذا المستوى |
| متجر المحتوى | نظام مشابه لـ Nix Store (hash-based) | إعادة إنتاج مضمونة 100% |
| العزل | namespaces + cgroups + seccomp، ثم Firecracker | يبدأ بسيط، يتطور لعزل بمستوى VM بأداء شبه أصلي |
| النفق (تشغيل=نشر) | خدمة WireGuard مدمجة | تشفير قياسي، خفيف، مثبت الموثوقية |
| الواجهة | WebKitGTK أو Chromium Embedded | يسمح لأي مطوّر ويب ببناء تطبيقات فورًا |
| البناء التلقائي | GitHub Actions | يبني ISO تلقائيًا ويختبره بـ QEMU headless عند كل push |
| الذكاء الاصطناعي | نموذج مفتوح الوزن (Mistral/Llama صغير) | يعمل محليًا أو على خادم خلفي، مجاني دائمًا، لا API مدفوع |

---

## 6. النموذج الهجين: محلي + سحابي

النظام **يُثبَّت فعليًا على القرص** (ISO حقيقي، ليس متصفحًا) ويعمل بالكامل بدون إنترنت. الطبقة السحابية **منفصلة تمامًا** وتضيف فقط:
- مساعد ذكاء اصطناعي (يستدعيه العميل المحلي عبر API بسيط عند توفر الإنترنت).
- متجر تطبيقات ومصدر تحديثات (يُسحب من GitHub Releases).
- مزامنة اختيارية للإعدادات بين أجهزة المستخدم.

**GitHub ليس خادم الاستضافة** — هو فقط مصدر الكود والبناء التلقائي والتخزين (Releases). الخدمة الخلفية الحيّة تُستضاف على خادم فعلي منفصل (مثل Oracle Cloud Free Tier، مجاني دائم فعليًا لا تجريبي).

---

## 7. نموذج المجتمع ومفتوح المصدر

- **الترخيص**: AGPL-3.0 (يضمن أي تعديل يبقى مفتوحًا حتى لو استُخدم على خادم بعيد — مناسب جدًا لمكوّن سحابي).
- **هيكل المستودع** (Monorepo مقترح):
  ```
  /kernel-config/     ← إعدادات Buildroot ونواة Linux
  /capsule-manager/    ← كود init البديل بلغة Rust
  /content-store/      ← منطق متجر المحتوى
  /shell-ui/           ← الواجهة (WebView shell)
  /cloud-backend/       ← خدمات الذكاء الاصطناعي والمزامنة (مشروع فرعي منفصل النشر)
  /.github/workflows/  ← أتمتة البناء والاختبار
  /docs/               ← هذه الوثائق وأي توثيق إضافي
  ```
- **الحوكمة**: نقاش القرارات الكبرى عبر GitHub Discussions/Issues، قرار نهائي لصاحب المشروع في البداية، ينتقل لمجلس مساهمين لاحقًا مع نمو المجتمع.

---

## 8. الذكاء الاصطناعي المدمج

- مساعد مدمج بالنظام (زر أو أمر طرفية) يفهم سياق النظام نفسه: يقدر يشرح خطأ إقلاع، يقترح كود لكبسولة، يشخّص مشكلة شبكة.
- **مجاني للأبد فعليًا** لأنه يعمل بنموذج مفتوح الوزن، لا اشتراك API مدفوع. البداية: نموذج صغير يعمل على الخادم الخلفي المجاني؛ لاحقًا: تشغيله محليًا كليًا إذا سمحت موارد جهاز المستخدم.

---

## 9. خارطة الطريق (مراحل بمعايير قبول واضحة)

| # | المرحلة | معيار القبول (كيف نعرف أنها خلصت فعلًا) |
|---|---|---|
| 0 | بيئة بناء تعمل (WSL2 + Buildroot) | `make menuconfig` يفتح بدون خطأ |
| 1 | ISO أدنى يقلع في QEMU | يظهر shell قابل للكتابة فيه |
| 2 | init بديل بلغة Rust (PID 1) | يقلع النظام بواسطته بدل BusyBox init الافتراضي |
| 3 | متجر محتوى + أول كبسولة تجريبية | كبسولة "hello world" تُبنى وتُشغَّل بمعرّف hash ثابت |
| 4 | عزل كبسولتين عن بعض | كبسولتان تعملان بنفس الوقت بدون تداخل موارد |
| 5 | نفق "تشغيل=نشر" | كبسولة محلية تصبح متاحة برابط شبكي فور تشغيلها |
| 6 | واجهة WebView أساسية | محرر + طرفية + قائمة كبسولات بواجهة واحدة |
| 7 | ربط الخدمة السحابية الخلفية | مساعد الذكاء الاصطناعي يستجيب فعليًا من داخل النظام |

كل مرحلة **يجب أن تعمل فعليًا وتُختبر في QEMU قبل الانتقال للتي بعدها.** لا قفزات.

---

## 10. مقاييس النجاح

- نظام يقلع فعليًا على جهاز حقيقي (لا فقط QEMU) — اختبار على الأقل جهازين مختلفين.
- مطوّر خارجي (غيرك) يقدر يبني ويشغّل كبسولة "hello world" خلال أقل من 10 دقائق من قراءة التوثيق.
- أول pull request خارجي مقبول من مساهم لا تعرفه شخصيًا.

---

## 11. المخاطر والتحديات (بصراحة كاملة)

- **الحجم الحقيقي للمشروع**: هذا عمل يأخذ من فريق متفرغ أشهرًا إلى سنوات للوصول لنسخة قابلة للاستخدام اليومي؛ كفرد واحد، الهدف الواقعي الأول هو **إثبات المفهوم (proof of concept)** يعمل، لا نظام تشغيل جاهز للمستهلك النهائي من أول نسخة.
- **التوافق مع العتاد**: دعم كل بطاقات الشبكة/الرسوميات عمل ضخم بذاته؛ نبدأ بدعم محدود (VGA أساسي، شبكة قياسية) وننمو تدريجيًا.
- **الأمان**: أي خطأ بطبقة العزل قد يعرّض كل الكبسولات للخطر؛ يجب اختبار أمني جاد قبل أي إعلان "جاهز للإنتاج".

---

## 12. الخطوة التالية المباشرة

استكمال المرحلة 0-1 من خارطة الطريق (نظام يقلع في QEMU) — راجع ملف `03-agent-instructions.md` للتنفيذ التفصيلي خطوة بخطوة.
